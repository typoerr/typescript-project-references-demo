{
  "name": "typescript-project-references",
  "description": "",
  "version": "0.0.1",
  "author": "typoerr",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^24.0.24",
    "@types/node": "^12.12.21",
    "@typoerr/eslint-config": "^0.0.22",
    "eslint": "^6.8.0",
    "jest": "^24.9.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.0",
    "ts-jest": "^24.2.0",
    "ts-node": "^8.5.4",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^3.7.4"
  },
  "scripts": {
    "start": "tsc -b -w -v",
    "build": "tsc -b -v",
    "clean": "tsc -b --clean"
  },
  "jest": {
    "preset": "ts-jest",
    "verbose": true,
    "globals": {
      "ts-jest": {
        "tsConfig": "test/tsconfig.json"
      }
    }
  }
}